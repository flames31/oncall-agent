global:
  scrape_interval:     15s
  evaluation_interval: 15s

rule_files:
  - "prometheus-rules.yaml"

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]

scrape_configs:
  - job_name: oncall-agent
    static_configs:
      - targets: ["agent:8080"]